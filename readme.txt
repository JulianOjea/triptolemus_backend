module is active in package json


en vez de "dev": "nodemon src/app.js" se puede usar "dev": "node --watch index.js" -> npm run dev

validaciones con zod
bcript para encriptacion de contraseña

dev run with: npm run dev